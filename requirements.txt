# Real but lightweight LLM (flan-t5-small, ~308MB, CPU-friendly)
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.1.99    # required by the T5 tokenizer

# Real RAG retriever (all-MiniLM-L6-v2, ~80MB embedding model)
sentence-transformers>=2.5.0
numpy>=1.24.0

# (later steps add no new heavy deps)
